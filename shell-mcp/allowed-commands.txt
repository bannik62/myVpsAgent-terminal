# Commandes autorisées pour le shell MCP
# Une commande par ligne — le préfixe suffit (ex: "ls" autorise "ls -la /etc")
# Laisser ce fichier vide ou le supprimer = toutes les commandes autorisées (non recommandé en prod)

# Fichiers et répertoires
ls
cat
tail
head
grep
find
du
df

# Système
free
uptime
ps
uname
hostname
env

# Logs
journalctl

# Réseau
curl
ping
ss
netstat
