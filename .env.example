# Token d'authentification (généré automatiquement par install.sh)
MCP_TOKEN=

# Port local du container MCP (choisi par install.sh)
MCP_PORT=

# Timeout des commandes en ms (défaut: 30000 = 30s)
# Augmenter pour docker build, apt upgrade, etc.
MCP_TIMEOUT=30000
